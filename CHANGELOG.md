# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) の体裁に従い、
バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従う。

## [0.10.7] — 2026-09-19

### 追加 — `drop_empty_rows`（**全セルが空欄の行を削除する**。既定は `False`）
`autoprep(..., drop_empty_rows=True)`、ノートブックでは `DROP_EMPTY_ROWS`。

★既定では 1 行も削除しない。★ 行を削除すると症例数と並びが変わり、
元のデータと症例ごとに `axis=1` で結合し直せなくなる。
それでも**全てのセルが空欄の行は症例ですらない**ので、消したい人が選べるようにした。

**黙っては消さない。**

- `True` のとき: `除外の記録.xlsx` に **「削除した行」シート**を足す
  （`元の行` / `Excel の行` / `理由`）。`res.dropped_rows` でも取れる
- 「まとめ」シートの**一番上**に置く。★本当に削除したものだけが取り返しがつかない★
- `書き出したデータの説明` の `行` 欄は「元と同じ」ではなく
  **「★空行 N 行を削除★」**になる
- `False` のときも、**空欄だけの行があれば段の表と警告で必ず知らせる**

### 修正 — ★読み込みが、表の途中の空行を黙って消していた★
`read_any` は `dropna(how="all")` で**無条件に**空行を消していた。
これでは `drop_empty_rows=False`（既定）が意味を持たない。**既定が効かない既定は嘘である。**

扱いを二つに分けた。

| どこの空行 | 扱い | なぜ |
|---|---|---|
| **表の末尾**にぶら下がったもの | 落とす（落としたと言う） | Excel の「使った範囲」の名残り。症例ではない |
| **表の途中**にあるもの | **残す**（あると言う） | 入力の事故。消してよいかは人が決める |

## [0.10.6] — 2026-09-19

### 変更 — 長い文の列は**横に広げずに折り返す**
0.10.5 では列幅を中身から決めたが、幅で解決すると
**「理由」1 列で画面が埋まり、隣の列が見えなくなる。**
説明の表では、40 桁を超える文の列に**折返し**を掛けることにした。

- 折返し列: 幅 40 桁で固定、**上下は中央・左右は左詰め**
  （折り返した文を中央揃えにすると、行ごとに文頭がずれて読めない）
- **行の高さは指定しない。** 指定しないほうが Excel が中身に合わせる
- 見出しの行は中央揃え＋折返し

★折返しを掛けるのは説明の表だけ。★ 症例レベルのデータに掛けると、
自由記載が 1 行あるだけで 600 行すべてが高くなり、**値を追えなくなる。**

| ファイル / シート | 折返し |
|---|---|
| `除外の記録.xlsx`（まとめ / 外すのが望ましい症例 / 解析から外す列） | ○ |
| `書き出したデータの説明.xlsx` | ○ |
| `掃除済みデータ.xlsx` の「列の扱い」「減らしたもの」 | ○ |
| `掃除済みデータ.xlsx` / `解析用データ.xlsx` / `前処理済み_*.xlsx` の「データ」 | × |

### 修正 — 掃除済みデータの 2・3 枚目に書式が掛かっていなかった
「列の扱い」「減らしたもの」の 2 枚は `_fmt_sheet` を通っておらず、
既定の幅のままだった。**`判断の根拠` が読めない。** 通すようにした。

## [0.10.5] — 2026-09-19

### 追加 — 「解析から外す列」のシート（**本当に消える列だけ**）
`table/除外の記録.xlsx` を **3 枚組**にした。3 枚目は
**解析用データから本当に消える列だけ**を並べる。

| シート | 中身 |
|---|---|
| まとめ | 何を、いくつ、なぜ減らしたか（列・行・値の集計） |
| 外すのが望ましい症例 | 1 症例 1 行。元の行・ID・理由・関係する日付 |
| **解析から外す列** | **列名・理由・どの段で消えたか** |

★「特徴量にしなかった（列は残る）」は入れない。★ 列の扱いには二通りある。

| 扱い | 解析用データに | 説明変数に |
|---|---|---|
| 解析から外した（列を削除） | **残らない** | 使わない |
| 特徴量にしなかった（列は残る） | 残る | 使わない |

日付や生存時間の列は後者で、そのままでは説明変数にならないが、
症例を確かめるのに要るのでファイルには残す。この二つを一緒に並べると、
**「消えた」と言われた列が手元のファイルにあって人が混乱する。**

`掃除済みデータ.xlsx` には全列がそのまま残る。あれは人が元の記録と
突き合わせるためのものだからである。消えるのは `解析用データ.xlsx` から先。

API: `mp.removed_columns(rep)` で同じ表が取れる。
処置の文字列は `COL_DROPPED` / `COL_UNUSED` の定数にした
（★表に書く文字と抽出に使う文字を二か所に書くと、片方だけ直したとき黙ってずれる★）。

### 修正 — Excel の列幅が**見出しだけ**で決まっていて文字が切れていた
`_fmt_sheet` が列幅を見出しの長さから決めていた。
「理由」のような**短い見出しに長い文が入る列**が必ず切れる。
隣のセルが空でなければ Excel は文字をはみ出させないので、
**切れた文字は画面から消える。** 理由が読めない一覧に値打ちはない。

中身の最大幅（★日本語は 2 桁分として数える★）から決めるようにした。
上限 70 桁。`除外の記録.xlsx` の「まとめ」でも
「解析から外した（列を削除）」が「解析から外」で切れていた。

## [0.10.4] — 2026-09-19

### 追加 — 「外すのが望ましい症例」の一覧を書き出す
印（`除外推奨` / `除外推奨_理由`）は掃除済みデータの 2 列に入っていたが、
**600 行の中から 22 行を探すのは人の仕事ではない。**

`table/除外の記録.xlsx` を **2 枚組**にした。

| シート | 中身 |
|---|---|
| まとめ | 何を、いくつ、なぜ減らしたか（列と行の集計。従来の内容） |
| **外すのが望ましい症例** | **1 症例 1 行**。元の行・ID・理由・関係する日付 |

★日付の列を添えてある。★ 除外の理由はほとんど日付の矛盾（両方記入・両方空欄・
逆転）なので、**理由だけ見せられても人は確かめようがない。**

同じ表は **`mp.excluded_cases(rep)`** でも取れる。

★行は削除していない。★ 外すかどうかは医学的な判断であって、自動化が黙って
決めることではない。この表は**人が決めるための材料**である。

### 教材
- 演習NB **Ver2_3** ―― 画面に出す症例数の上限を 10 → **100** にした。
  ファイルにも出ていることと、その場所を書き添えた。

## [0.10.3] — 2026-09-19

### 追加 — `前処理済み_*.xlsx` に sheet3「欠損値の位置」
補完する**前**に、どこが空欄だったかを 0/1 でそのまま残す。

```
1 = その症例のその項目は空欄だった（このあと中央値などで埋めた）
0 = 実測値がそのまま入っている
欠損数 = その症例に空欄がいくつあったか（★大きい行は症例そのものを疑う★）
```

### 変更 — ★欠損指示子を既定では特徴量にしない★
「測っていない」こと自体が情報であることはある（安定していたから測らなかった、
重症で測れなかった）。しかし**多くの欠測は単なる入力漏れ**で意味を持たない。
欠測率 0.5% の列の指示子はほぼ定数になり、**正則化モデルを不安定にするだけ**である。

- `policy["missing"]["add_indicator"]` の既定を **False** にした。
- **情報は 1 つも失われない。** 欠測の位置は「欠損値の位置」シートに 0/1 で残る。
  意味のある欠測だと判断したら、その列だけを結合して特徴量にすればよい。
- 全列に作らせたいときは `policy={'missing': {'add_indicator': True}}` と
  **明示する**。そのときは従来どおり `欠損あり_〇〇` という名前で出る。

★これまで `欠損あり_〇〇` は標準化されていた。★ 同じ 0/1 の旗なのに、
ダミー変数（`施設=B院`・`男性`）は 0/1 のままで指示子だけ z 値になっていた。
設計判断ではなく、標準化が数値の枝だけを通るという置き場所の事故である。
既定で作らなくなったので、この食い違いも消えた。

### 教材
- 演習NB **Ver2_2**

## [0.10.2] — 2026-09-19

### 修正 — ★「train / test を作らなかった」ことを言っていなかった★
目的変数（`outcome`）を指定しないと分割も前処理も行わない。これは正しい
（何を予測するのか決まっていなければ、分割にも標準化にも意味がない）。
しかし **その段を記録にすら残していなかった。**
利用者からは「`前処理済み_train.xlsx` が無い」としか見えず、**理由が分からない。**

このパッケージは「飛ばした段は黙って消さない」ことを原則にしているのに、
ここだけ抜けていた。

- 段として残す:
  `train / test に分ける — ★目的変数（OUTCOME）の指定が無いので分割しない。
  data/前処理済み_train.xlsx と _test.xlsx は作られない★`
- `rep.notes` にも、どうすれば作られるかを書く。
- **`書き出したデータの説明.xlsx` に「作っていない」行を出す。**
  空欄は「無い」の説明にならない。
- 同じ表に `data/元データ.xlsx` の行も足した（0.10.1 で保存を始めたのに、
  説明表に載っていなかった）。

## [0.10.1] — 2026-09-19

### 追加 — 透析年数（★切り捨て★）
透析開始年月日があれば、**その検査の日まで**の年数を `透析年数` として作る
（`derive_vintage`）。日付を解釈したあとに走る段を 1 つ足した。

- 終了日は 検体採取日 →（無ければ）観察開始年月日 の順で選び、
  **どちらを使ったかを必ず注記に出す**。
- **今日の日付では代用しない。** 走らせた日で値が変わる列を黙って混ぜてはならない。
  終了日が無ければ作らない。
- 0 年は「導入後 1 年未満」。導入日が不明・開始 > 終了の症例は NaN。
- 辞書に `vintage_y`（透析年数、単位 年、0〜60）を足した。月単位の `vintage` とは別項目。

### 変更 — 合成データ
- **`透析歴_月` を渡すのをやめ、`透析開始年月日` を渡す。**
  実務のデータは導入日が入っていて、年数は入っていない。透析歴は年単位で作らせる。
- **`C反応性蛋白(CRP)定量` → `CRP定量`。** 列名が長く、Excel で切れると読めなかった。
- 導入日が空欄の症例を汚れとして入れてある。

### 変更 — Excel の小数は最低 2 桁
0.10.0 の桁数計算は「いちばん小さい非ゼロの値 + 1 桁」だったので CRP が 3 桁になった。
**下限を 2 桁**にした（CRP → `0.00`、Alb → `0.00`、duration → `0.000`）。

### 追加 — `前処理済み_*.xlsx` を 2 枚組にした
| シート | 中身 |
|---|---|
| **標準化後** | モデルに渡す行列。施設は `施設=B院` のダミー、数値は z 値 |
| **生の値** | ★同じ症例・同じ列を、標準化する前の値で並べたもの★。施設は `A院` のまま |

標準化後の表は計算のためのもので、人が読んでも意味が取れない。
**群間比較をしたり値を目で確かめたりするには「生の値」のシートを使う。**

### 追加 — `data/元データ.xlsx`
前処理前の、手を触れていない元データも保存する。
**掃除の前後を突き合わせられないと、「この値はもともとこうだったのか、
機械が直したのか」が分からなくなる。**

### 教材
- 演習NB **Ver2_1**

## [0.10.0] — 2026-09-19

### 修正 — ★外れ値処理が列を丸ごと潰していた（透析時間が全例 0 になる）★
`iqr_limits()` が **IQR = 0 を弾いていなかった。**
値が数種類しかない列では Q1 = Q3 になる。透析時間は 4.0 時間が 6 割を占めるので、
まさにこれに当たっていた。上下限が同じ値になり、**列全体が 1 つの値に潰れる。**
スケーリング後は全例きっかり 0 になり、`前処理済み_train.xlsx` の
`透析時間(hr)` は標準偏差 0 の列だった。

**例外は出ない。** 図も表も正常に見えたまま、その変数だけがモデルから消える。
MAD 法は同じ理由で 0 を弾いていたのに、IQR 法だけ抜けていた。

- `iqr_limits()` は IQR = 0 のとき閾値を作らない（`±inf` を返す）。
- **`Preprocessor.fit()` が、出来上がった行列に値が 1 つしかない列が無いかを
  見張る。** あれば `record.warnings` に出す。同じことが別の経路で起きても気づける。

### 変更 — 合成データの透析時間を連続にした
3.5 / 4.0 / 4.5 / 5.0 の 4 値しか取らなかったものを、**15 分刻み・中央値 4.0 時間・
3.0〜5.5 時間**に広げた。実務のばらつきに合わせ、かつ IQR が 0 にならないようにする。

### 修正 — Excel で小数が見えなくなっていた
書式を指定していなかったため、表示環境によっては CRP の 0.05 が「0」に見えた。
**列ごとに、いちばん小さい非ゼロの値が 2 桁の有効数字で読める桁数**を当てる
（CRP → `0.000`、Alb → `0.0`、年齢 → `0`）。列幅も見出しの長さに合わせる。

### 変更 — 検出限界の印の列名を日本語にした
`C反応性蛋白(CRP)定量__censored_low` は、Excel で列名が切れると **「CRP」に見え、
ほぼ全部 0 なので「CRP が全部 0」と読まれる。**
`検出限界未満_C反応性蛋白(CRP)定量` に変えた（`__censored_high` も同様）。

### 修正 — 欠損の地図の列名が重なって読めなかった
40 列の日本語の列名を既定の大きさ・45 度で描くと、**文字どうしが重なって 1 文字も
読めない。** 図としては描けているので例外は出ない。

- 列数から**図の幅**を決め（1 列 0.34 インチ、最大 26 インチ）、
- **1 列あたりの幅に収まる字の大きさ**を計算して当て、
- 列が多い・名前が長いときは**縦書き（90 度）**にする。横に要る幅が字 1 つぶんで済む。

### 教材 — 演習NB Ver2_0
- 列を選ぶ画面に **［確定する］** ボタンを足した（［すべて指定しない（skip）］の左）。
  **押さなくても同じ**である（選んだ時点で反映される）。押した場合は「確定した」と出す。
- **何も操作しなければ、推定した既定の選択がそのまま有効になる**という動きは
  そのまま残してある。操作が分からなくても計算が進むための保険である。

## [0.9.4] — 2026-09-19

### 追加 — ★算出した観察期間とイベントを、データに列として残す★
生存時間の 3 列（観察開始日・イベント発生日・打ち切り日）を指定したのに、
**書き出したデータに観察期間もイベントも入っていなかった。**
`rep.survival.data` の中にしかなく、Excel を開いた人には見えない。
「指定したのに出てこない」のでは、指定した意味がない。

掃除済みデータと解析用データに次の 4 列を足す。

| 列 | 中身 |
|---|---|
| `duration` | 観察期間（既定は年） |
| `event` | 1 = イベント発生、0 = 打ち切り |
| `_start` | 解釈済みの観察開始日 |
| `_end` | 解釈済みの観察終了日（イベント日または打ち切り日） |

- 生存時間に変換できなかった症例は **NaN**。★行は削除しない。★
- 役割は `TIME` / `EVENT` にしてある。`Schema.features()` はこの 2 つを外すので、
  **目的変数そのものが説明変数に紛れ込むことはない。**

### 教材
- 演習NB **Ver1_9_1**

## [0.9.3] — 2026-09-19

### 修正 — 列を選んだら**その場で反映**する（ボタンもセルの再実行も不要）
0.9.0 の選択画面は「この指定で進む」ボタンを押して初めて反映される作りだった。
**選んだあと何をすればよいのかが画面から読み取れない。**

- ドロップダウンを `observe` し、**選んだ瞬間に `GROUP` などへ反映**する。
- 表示した直後に一度当てはめるので、**推定した初期値（施設・日付 3 列）も
  触らずに効く**。
- `（指定しない / skip）` に戻せば、その場で解除される。
- 画面に「★選んだ時点で反映される。このセルを実行し直す必要はない。★
  次は演習①のセルを実行すること」と出す。

### 修正 — Colab でボタンが出ないことがあった
`display(VBox, HBox, Output)` のように**複数のウィジェットを並べて渡す**と、
Colab では 2 つめ以降が描画されないことがある。実際、選択画面の下の
ボタンが出ていなかった。**1 つの `VBox` にまとめて display する**ようにした。

### 修正 — 生成したノートブックが ruff に通らなかった（CI が赤くなった）
0.9.3 で使わなくなった `import contextlib` がセルに残ったまま出て行き、
**CI の ruff で初めて気づいた**。ruff は `.ipynb` も検査するが、生成側の
`build_notebook.py` はセルの中身を**文字列**として持っているので、
生成側に ruff を掛けても見つからない。

- **`build_notebook.py` が、作った直後の `.ipynb` に自分で ruff を掛ける**
  ようにした。通らなければその場で止まる。CI まで持ち越さない。

### 教材
- 演習NB **Ver1_9**（Ver1_8_3 と中身は同じ。版番号を整えたもの）
- `NOTEBOOK_NAME` を **`VERSION` から自動で入れる**ようにした。
  手で直す作りだったので `Preprocessing_Ver1_0` のまま置き去りになっていた。
- 演習NB **Ver1_8_3**（未使用 import を外して作り直した。動作は同じ）

## [0.9.2] — 2026-09-19

### 修正 — 表のセルを**上下方向にも**中央にした
左右（水平）は中央にしていたが、上下（垂直）が既定のままだった。
Word のセルは上詰め、Excel のセルは下詰めが既定なので、
**2 行に折り返したセルと 1 行のセルとで文字の高さが揃わない。**
項目名が 2 行になる行（`透析前クレアチニン(Cr) (mg/dL)` など）で目立つ。

- Word … 全セルに `vertical_alignment = CENTER`
- Excel … 見出し・本文とも `vertical="center"`（従来どおり）で、行の高さを持たせた
- HTML … `vertical-align:middle`（Word に貼ったときの見えかたを合わせる）

### 教材
- 演習NB **Ver1_8_2**

## [0.9.1] — 2026-09-19

### 修正 — iCa を Payne 式（条件つき）に戻した
0.9.0 で `iCa = Ca + (4 − Alb)` を**無条件**にしたが、**Alb ≥ 4.0 では補正しない**
のが日本の運用である。無条件にすると Alb 4.5 の症例で実測 Ca より 0.5 低くなる。

    Alb < 4.0   iCa = Ca + (4 − Alb)
    Alb ≥ 4.0   iCa = Ca（実測のまま）

`audit` の検算も同じ式に戻した。Alb が欠測なら **算出不能（NaN）** のままである
（`np.where(np.nan < 4.0, …)` は False に落ちるので、必ず mask している）。

### 変更 — ★Table 2 から「全体」の列を外した★
2 枚に分けた意味が無くなるためである。

    Table 1 … 全体
    Table 2 … 群間比較（群ごとの列と p 値だけ）

横にも 1 列ぶん短くなり、群が多いときに読みやすくなる。
N は各群の見出し（`A院 (N = 230)`）に入っているので、情報は落ちない。

### 教材
- 演習NB **Ver1_8_1**（Ver1_8 からの小修正。上書きはしない）

## [0.9.0] — 2026-09-19

### 追加 — 論文にそのまま載る Table 1 / Table 2（`medprep.tables`）
`table_one()` は「全体の記述」と「群間比較」を 1 枚に詰めていた。読む側の目的が
違うのに 1 枚だと、どちらも読みにくい。論文でも普通は分ける。**2 枚に分けた。**

| | 中身 | p 値 |
|---|---|---|
| Table 1 | 全症例の背景 | ★載せない★ |
| Table 2 | 全体 + 群ごと + 群間比較 | 右端に 1 列 |

- **`gt_tables(df, schema, group=...)`** → `GTSummary`（`.table1` / `.table2`）。
  `group` の指定が無ければ **Table 1 だけ**を作る。
- 書式は gtsummary に合わせた。連続変数は **平均値 ± 標準偏差 [最小値, 最大値]**、
  離散変数は **n (%)**。**検定手法は表の中ではなく脚注**に出す
  （行ごとに書くと表が横に伸びて読めなくなる。しかし書かない表は査読に通らない）。
- **日本語版と英語版を同じ数値から作る**（`medprep.i18n` に対訳表）。
  人が訳し直すと、そこで誤訳と写し間違いが入る。
- 書き出し
  - `table/Table1_2.xlsx` … **sheet1 = 日本語 / sheet2 = 英語**
  - `table/Table1_2.docx` … **ページを分けて日本語 → 英語**。表は中詰め・三本罫線。
    群が多くて横に伸びる表だけ**自動で横置きページ**にする。
  - `GTSummary.to_html()` … notebook に出すと、**選んでコピーして Word に貼れる**。
- HTML レポートに「5. Table 1 / Table 2」の節を追加。
  従来の詳細版は「5a. Table 1（詳細版）」、ファイル名は `table1_詳細.xlsx` に変えた。

### 追加 — 透析の派生指標を、揃っている列の組についてだけ自動で作る
| 作る列 | 要る列 | 式 |
|---|---|---|
| `除水量(kg)` | 透析前体重・透析後体重 | 前 − 後 |
| `URR(%)` | 透析前BUN・透析後BUN | (前 − 後) / 前 × 100 |
| `透析時間(hr)` | 透析開始時刻・透析終了時刻 | 終了 − 開始（日またぎは +24h） |
| `spKt/V` | 前後BUN・除水量・透析後体重・透析時間 | Daugirdas 第2世代式 |
| `TSAT(%)` | Fe・TIBC | Fe / TIBC × 100 |
| `iCa(mg/dL)` | Ca・Alb | Ca + (4 − Alb) |

- **揃っていない組からは作らない。** 片方だけで推測した値を入れると、
  それが計算値か実測値か、あとから誰にも分からなくなる。
  既に同じ名前の列があるときも作らない（人が入れた値を上書きしない）。
- 時刻は `9:00`・`08:30`・`8時45分`・全角コロン・Excel のシリアル小数を読む。
  **読めないものを 0 にはしない**（NaN にして件数を報告する）。
- **spKt/V の UF は L で入れる。** 水 1 L = 1 kg なので `除水量(kg)` をそのまま使う。
  mL で入れると 1000 倍になり Kt/V が 2000 になる。脚注にも書いた。

### 変更 — ★補正Ca の式と列名★
- 列名 `補正Ca` → **`iCa(mg/dL)`**、`補正Ca×P` → **`iCa×P`**（単位を列名に書く）。
  古い名前は辞書の別名に残してあるので、読み込み側は従来どおり。
- 式を **`iCa = Ca + (4 − Alb)`（無条件）** に変えた。
  0.8.0 までは「Alb < 4.0 のときだけ補正する」という運用に合わせていた。
  **Alb ≥ 4 の症例で値が変わる**（例: Ca 9.0 / Alb 4.2 → 9.0 だったものが 8.8）。
  `audit` の検算も同じ式に合わせた。

### 変更 — 合成データ
- **`透析時間` を渡すのをやめ、`透析開始時刻` / `透析終了時刻` にした。**
  実務のデータはたいてい時刻で入っていて、時間そのものは入っていない。
  表記ゆれ（`9:00`・`8時45分`・全角コロン）も混ぜてある。
- `血清鉄(Fe)` / `総鉄結合能(TIBC)` を追加（TSAT を算出させるため）。33 列になった。

### 教材 — 演習NB Ver1_8
- **GROUP / OUTCOME / TASK / SURVIVAL_DATES をクリックで選べるようにした。**
  環境変数に書いてあればそれが最優先。**空のときだけ**、4) でデータを読んだ直後に
  列名を全部並べて選択画面を出す（skip もできる）。自動実行（`LOCAL_DATA_PATH` を
  書いたとき・D&Dアプリ）は待たずに推定値を当てはめる。
- 演習⑤を「Table 1 / Table 2 ―― そのまま論文に貼れる形」に差し替えた。
- 既定を **`TEST_SIZE = 0.2` / `RANDOM_STATE = 42`** にした。

## [0.8.0] — 2026-09-19

### 修正 — ★合成データが作り直されず、古い列のまま使われていた★
演習NB の［演習用の合成データを使う］は「ファイルが**無ければ**作る」だった。
そのため **medprep の版が上がって列が増えても、前に作った古いファイルが
そのまま読まれ続けた。** 0.6.0 で足した透析前後の 2 点（BUN・Cr・K・体重）・
身長・透析時間が、実機では一度も現れていなかった。

- **押すたびに作り直す**ようにした。合成データは seed 固定なので中身は同じ。
  作った版（`medprep x.y.z`）と、透析前後を含むことを画面に出す。

### 変更 — 解析用データでも二値の列を 0/1 にする
0.7.0 の二値符号化は `Preprocessor`（＝モデルに渡す行列）の中だけで効いていた。
そのため **`前処理済み_*.xlsx` では `男性` が 0/1 なのに、
`解析用データ.xlsx` では `性別` が 男/女 のまま**という食い違いが残っていた。

- **`encode_binary_columns(df, schema)`** を追加。`Preprocessor` と**同じ規則**を
  素の DataFrame に掛ける。列は**同じ位置で置き換える**（並びは変わらない）。
  対応表に無い値は**勝手に 0 にせず**欠損にし、件数を報告する。
- `autoprep()` に段「二値の列を 0/1 に直す」を追加。`rep.df_use`（解析用データ）と
  `rep.encoded`（何を 1 にしたかの表）が増えた。
  `解析用データ.xlsx` の 2 枚目に「0と1の対応」を入れる。
- **`掃除済みデータ.xlsx` には掛けない。** あれは人が元の記録と突き合わせる
  ためのものなので、`男/女` のまま残す。HTML レポートの新しい節
  「1d. 二値の列を 0/1 に直した」にこの区別を書いた。

### 教材 — 演習NB Ver1_7
- **演習④「透析前後の 2 点 ―― どちらが『前』かを推測で決めない」を新設**
  （以降の演習番号が 1 つずつ繰り下がる）。
  前後の列を目で確かめ、`TimingSchema.infer` で採血時点を対応づけ、
  **1 施設だけ前後が入れ替わっている**のを施設別集計で見つけ、
  URR・spKt/V を出して**その施設だけ URR が負になる**のを確かめる。
- 演習①に「二値の列を 0/1 に直した」の節を追加（`rep.encoded` と `rep.df_use`）。

## [0.7.0] — 2026-09-19

### 変更 — ★二値の列は 0/1 の 1 本にまとめ、1 が何かを列名で示す。★
性別のように水準が 2 つの列は、one-hot にしても列は 1 本にしかならない。
問題は**どちらの水準が 1 になるか**で、`OneHotEncoder(drop="first")` は
それを **sklearn の辞書順**で決めていた。結果はこうなる。

    男/女   で記録した施設 → 列名 `性別=男`
    M/F    で記録した施設 → 列名 `性別=M`
    男性/女性 で記録した施設 → 列名 `性別=女性`（!)

**同じ意味の列が、書き方によって別名になり、しかも 1 がどちらかが読めない。**
施設をまたいで結合した瞬間に破綻する。

- **`BinaryMapper`**（`pipeline.py`）を足し、`schema` が見つけた対応表
  （`ColumnSpec.value_map`）で 0/1 に直す。**男性=1・女性=0、列名は `男性`。**
  書き方（男/女・男性/女性・M/F・Male/Female）が違っても**同じ列名**になる。
- **`binary_output_name()`**（`schema.py`）— 1 側の水準名が
  それだけで意味の通るもの（男 → **男性**、死亡 → **死亡**）だけを列名にする。
  `あり/なし`・`陽性/陰性`・`1/0` は **元の列名のまま**にする。
  列名を「あり」にしたら、何の列か分からなくなるからである。
- **学習するものが無い。** 対応表は固定なので、train に片方の水準しか
  現れなくても test の向きが変わらない。one-hot にはこの事故があった。
  対応表に無い値は欠損として後段の補完に渡す（例外にしない）。
- `reference_levels()` は二値列についても **0 とした水準**を返す。
  HTML レポートの「基準水準」の表に **作った列**の欄を足した。

水準が 3 つ以上の列（施設 A院/B院/C院）はこれまでどおり one-hot である。

### 教材
- 演習 NB **Ver1_6** — 「カテゴリはどう数値になったか」の節を足した。
  出てきた列と基準水準を出し、**元の性別と `男性` 列を突き合わせて
  向きを自分の目で確かめる**セルを置いた。

## [0.6.0] — 2026-09-19

### 追加 — 「書き出した 3 つのデータはどう違うのか」を明記する
- **`rep.outputs`** — ファイルごとに 行 / 列 / **施した処置** / 使いどころ を並べた表。
  同じデータを 3 段階で書き出しており、**施した処置が 1 段ずつ違う。**
  どれを使うかで結果が変わるのに、それが分からないまま渡されるのがいちばん困る。
  - 掃除済み … 人が読む（ID も落とす予定の列も残す）
  - 解析用 … 自分で解析する（列は落とす。**行は減らない**）
  - 前処理済み … モデルに渡す（**⑨ でだけ行が減る**。元の行番号と ID を付ける）
- HTML レポートに「**1c. 書き出したデータ ―― 3 つはどう違うのか**」の節を足した。
  そのために **run フォルダを作ってからレポートを組む**ように順序を変えた
  （`_open_run` と `_write_all` に分割）。保存先のパスもレポートに載る。
- `table/書き出したデータの説明.xlsx` を保存する。

### 追加 — 合成データに透析前後の検査値
- **BUN・クレアチニン・カリウム・体重に透析前と透析後の 2 列**を作った。
  身長・透析時間も足した（30 列）。これで **URR・spKt/V・nPCR・%CGR・GNRI・BMI が
  算出可能**になり、`timing` と `hd` が演習で使えるようになる。
- **汚れを 1 つ足した：1 施設（C院）だけ前後の列が入れ替わっている。**
  実務でいちばんよくある形である（施設ごとにエクスポートの仕様が違う）。
  透析後 BUN のほうが高いのは生理学的にあり得ず、URR が負になる。
  `audit` の「前後の方向」検査が拾い、施設別に見ると 1 施設に偏っていると分かる。

### 修正
- **Excel の日付に `00:00:00` が出ていた。** `datetime64` のまま書くと既定書式が
  `YYYY-MM-DD HH:MM:SS` になる。書き出すときだけ日付に落とす
  （計算に使う `df_clean` は `datetime64` のまま。日付の引き算に要る）。

## [0.5.0] — 2026-09-19

### 変更 — ★行を削除しない。印を付けるだけにする。★
行を削除すると症例数と並びが変わり、**元のデータと症例ごとに `axis=1` で
結合し直せなくなる。** 外すかどうかは医学的な判断であって、自動化が黙って
決めることではない。

- `rep.df_clean` は**元と同じ行数・同じ並び**を保つ。
- 代わりに 2 列を足す。
  - `除外推奨`（0/1）… 解析から外すのが望ましい症例
  - `除外推奨_理由` … その理由（複数あれば「／」で連ねる）
  - この 2 列は**特徴量にしない**（人が読むための列である）。
- `rep.n_excluded` で印の数が分かる。
- `data/解析用データ.xlsx` も**行が 1 つも減らない**。印の 2 列が必ず付く。
- `rep.removed` に「処置」の列を足した。列は「解析から外した」、
  行は「★印だけ付けた（行は削除しない）★」、値は「NaN にした」。
- **`rep.X_train` / `X_test` だけは部分集合**（目的変数が欠測の症例では学習できない）。
  書き出すファイルに `元の行` と ID の列を付け、元データに戻せるようにした。

## [0.4.0] — 2026-09-18

### 追加 — 日付を解釈する
- **`autoprep` が日付の列を `datetime` に揃えるようになった。**
  掃除（`clean_numeric`）は**数値列にしか掛からない**。日付を別に直していなかったので、
  和暦 `H24.7.2`・全角 `２０１５／６／７`・Excel シリアル値 `41098`・時刻付き
  `2015-9-15 09:40:38` が**書き出したファイルに生の文字列のまま残っていた。**
  - `rep.dates` に列ごとの結果（並び・解釈できた件数・失敗した値）が入る。
  - ★日と月の順序が決まらない列（`3/4/15` のように年が末尾で日・月とも 12 以下）は
    **書き換えない。** 3月4日か 4月3日か分からないまま観察期間を計算すると
    最大 11 か月ずれる。人に返す。
  - 日付として読めた割合が半分に満たない列にも手を触れない。

### 変更 — 書き出すデータの形
- `data/掃除済みデータ.xlsx` を**3 枚のシート**にした。
  「データ」（全列・掃除済み・日付解釈済み）／「列の扱い」／「減らしたもの」。
  **落とすと決めた列が、保存したファイルから分かる**ようにするため。
- `data/解析用データ.xlsx` を足した。**落とすと決めた列を除いた**もの。
  掃除済みから ID を抜かないのは、**人が症例を辿れなくなるから**である。

## [0.3.0] — 2026-09-18

### 追加 — 「何を減らしたか」を明示し、データを書き出す
- **`rep.removed`** — 列・行・値のどれを、いくつ、なぜ減らしたかの一覧。
  **全自動の値打ちは「何をしたか」と同じくらい「何を捨てたか」で決まる。**
  捨てたものを言わない自動化は、信用してはならない。
  - 列：役割の推定で外したもの（ID・重複列・自由記載）と、特徴量にしなかったもの（日付）
  - 行：目的変数が欠測の症例、生存時間に変換できない症例（理由別）
  - 値：辞書で NaN にしたもの（欠損コード・あり得ない値。**行は消えない**）
  - `rep.report()` と HTML レポート（「1b. 減らしたもの」）にも出る。
  - **行の削除は段によって効く範囲が違う**ことを本文で断っている。
    「生存時間の形にする」で除いた例はモデルに渡す行列には影響しない。
- **前処理済みデータの書き出し**（`save=True` のとき既定で有効。`save_data=False` で止める）
  - `data/掃除済みデータ.xlsx` — 辞書で掃除し派生列を足した、**人が読める**データ
  - `data/前処理済み_train.xlsx` / `_test.xlsx` — スケール・符号化まで済んだ行列
    （目的変数の列を末尾に付ける）
  - `table/除外の記録.xlsx`
  - ★どれも症例レベルのデータである。★ `run` の根の `.gitignore` は従来どおり置く。
- `Preprocessor.input_columns()` — 前処理に実際に投入した元の列。

## [0.2.1] — 2026-09-18

### 修正
- **文字で出す表の桁が、日本語の列名でずれていた。**
  `DataFrame.to_string()` は桁を**文字数**で揃えるが、日本語は等幅フォントで
  2 桁ぶんの幅を取る。「アルブミン(Alb)」（9 文字・幅 14）と「年齢」（2 文字・幅 4）が
  同じ桁数として扱われ、表が崩れていた。**医学データの列名はほぼ日本語なので、
  ずれない表のほうが珍しかった。**
  `textfmt.frame_text()` を足し、報告のあいだだけ pandas に表示幅で揃えさせる
  （`display.unicode.east_asian_width`。`option_context` で囲い、利用者の設定は壊さない）。
  `schema` / `quality` / `describe` / `missing` / `outliers` / `splitting` /
  `survival` / `timing` / `clean` のすべての報告に適用した。
  **「全行の表示幅が同じか」をテストで見張る。**
- あわせて、長い値を切り詰めないようにした（`to_string` には `display.max_colwidth` が
  効かないため、引数で明示する）。「判断の根拠」「対応」は切り詰めたら意味を失う。

## [0.2.0] — 2026-09-18

Phase 11（`autoprep` の結線・`paths`）と Phase 12（第3回の教材）をまとめて。

### 変更（0.2.0）
- **版番号の出どころを 1 つにした。** `pyproject.toml` の `version` を廃し、
  `src/medprep/__init__.py` の `__version__` から取る（hatchling の dynamic version）。
  2 か所に書くと必ずずれる。ずれると、**Colab の受講者が「新しいはずなのに古い medprep」を
  掴んでも誰も気づけない。**
- 演習NBの導入セルに **`-U --no-cache-dir` と版の検査**を入れた。
  古ければ「ランタイムを再起動してから流し直すこと」と**理由を言って止める**。
  （版番号が同じままだと pip は取りに行かない。読み込み済みの古いモジュールは
  再起動しないと入れ替わらない。）

### 追加（Phase 12）— 第3回の教材
- `examples/notebook/Preprocessing_Ver1_0.ipynb` — **第3回演習ノートブック**（63 セル）。
  - 既存教材の作法に準拠（Colab マウント → 環境変数の一括設定 → ライブラリ導入 → データ入力）。
  - 演習①〜⑥：全自動 → schema の検分 → 欠損と外れ値 → Table 1 → 生存時間 → モデルに渡す。
  - **`nbclient` で実際に通して 0 エラーを確認してある。**
- `examples/slides/第3回_データの前処理_ver1_0.pptx` — **講義スライド 20 枚**。
  - 既存教材（`HowToSetUpPC.pptx`）の配色・座標に合わせた。すべて Bold。
  - `build_slides.py` / `deck.py` で組み立てる。**高さは本文の量から計算**し、
    各要素が次を置く位置を返す。文言を直しても崩れない。
- `medprep.demo` — **演習用の合成透析コホート**をパッケージに入れた。
  - `dialysis_cohort()` / `save()` / `TRUE_COEFFICIENTS`（Cox の真の係数）。
  - 仕込んだ汚れ 11 種をテストで固定した（消えていれば演習が成立しない）。
  - Colab では `pip install` した medprep からそのまま呼べる。

### 修正（Phase 12）
- `autoprep` — **`survival_dates` から作った `SurvivalFrame` が共変量を持っていなかった。**
  ID と (duration, event) だけでは Cox に掛けられず、利用者が元データと結合し直すことになる。
  日付列を除く特徴量を必ず持たせる。
- `autoprep` — **`columns`（モデルに入れる特徴量）を Table 1 にも渡していた。**
  日付や自由記載まで表に並び、Table 1 が 2,221 行になっていた。`table1_columns` を分けた。
- `describe.table_one` — **水準の多い列を 1 行ずつ展開していた。**
  自由記載の列が混ざると表が数百行になり、χ² も意味を持たない。
  `max_levels`（既定 20）を超えたら 1 行にまとめ、注記に理由を残す。

### 追加（Phase 11）— 層1の結線と保存先
- `autoprep()` / `quicklook()` — **1 行で読み込みから前処理済み行列とレポートまで。**
  - **層2を順に呼ぶだけの薄い層。層1に固有のロジックは 1 行も無い。**
    受講者が層1で見た結果を、層2で 1 段ずつ分解して追体験できる。
  - 背骨（読む・役割の推定・監査・分割・前処理）で失敗したら**止まる**。
    付随するもの（管理目標・生存時間・図）は**理由を残して飛ばす**。
    `rep.steps` に全段の成否が残る。
  - `rep.warnings` に人の確認が要る事項が集まる。**空で返るほうを疑うこと。**
  - `rep.X_train` / `rep.pipeline` / `rep.schema` / `rep.html` などから結果を取り出す。
- `paths` — **既存教材の `run{N}` 規約をそのまま使う。**
  - `~/lab_output/{手法名}/run{N}/{table,figure,model,report}/`、
    Colab は `{COLAB_BASE}/lab_output/...`、ログは `~/lab_work/log`。
  - `determine_runnumber()` / `create_directory()` は**既存ノートブックからの写し**。
    引数を変えないことをテストで固定した（D&Dアプリ互換）。
  - **採番に `report` を数えない**（既存ノートブックは table/model/figure だけを見る）。
  - `history.csv` は「既存の列 ＋ 未知の列」で作り直す。
    **ノートブック実行の「最良R2」を消さない。**
  - 結果フォルダの根に `.gitignore`（全無視）を必ず置く。
- `loading.read_any()` — csv / xlsx / parquet / json / sav / dta。
  - 文字コード（cp932 など）と見出し行を推測し、**推測を必ず記録に残す**。
  - 空の列・行を落とし、同名の列を警告する。
- `viz.FigureSet.save_all()` — 図を一括保存（Windows で使えない文字を避ける）。

### 修正（Phase 11）
- `report` — 列数の多い表（schema の 8 列）が枠から溢れ、
  **いちばん右の「判断の根拠」が横スクロールに隠れていた。** `max-width: 100%` で収めた。

### 追加（Phase 10）— 図と HTML レポート
- `viz` — **1 つの検証済み色体系で図を描く。**
  - 色は仕事で決める。**相関（符号あり）は発散配色で中点は灰色、
    関連の強さ（0〜1）は順次配色**。取り違えると中点の灰色が「負の関連」に見える。
  - カテゴリ配色は 8 色で打ち止め。`series(8)` は例外を投げる（**9 色目を作らない**）。
    水準が多ければ「その他」にまとめる。**散布図・PCA は 3 色まで**。
  - 図: 欠損の地図 / 連続変数の分布 / カテゴリの内訳 / 相関 / 関連の強さ /
    主成分 / 目的変数との関係 / 単変量 AUC / SMD フォレスト / 管理目標の達成率。
  - `overview()` が一通り作り、**描けなかった図は理由を残して飛ばす**。
  - **日本語フォントに無い文字（`≥` `≤` `✗` `µ`）を自動で置き換える。**
    そのまま描くと例外を出さず豆腐（□）になる。`missing_glyphs()` が検査する。
- `report` — **すべてを HTML 1 枚にまとめる**（図は base64 埋め込み）。
  - **警告を先頭に置く。** 冒頭に件数の帯と致命的所見、そこから本文へ降りる。
  - **症例レベルの値を既定で出さない。** レポートはメールで回るため。
    `show_values=True` を明示したときだけ値を出し、その旨をレポート自身が表示する。
  - 図には必ず同じ内容の表を添える（色だけに頼らせない）。
  - `to_excel()` で表をシート別にも出す。

### 修正（Phase 10）
- `quality` の測定法変更の所見で、対応と「既知の影響」が空白 2 つで繋がっており、
  HTML では 1 つに詰まって読めなかった。括弧に入れた。

### 追加（Phase 9）— リークを構造的に不可能にする
- `pipeline` — **前処理を 1 つの sklearn 変換器にまとめ、fit を train にしか呼べなくする。**
  - `Preprocessor.fit(test)` は **`LeakageError` で止まる**（`split()` が付けた印を見る）。
  - 分割を経ていないデータへの fit は警告する（最も多いリークの形）。
  - fit した行と transform する行が部分的に重なれば警告する（train と test の混在）。
  - `leak_check()` — 印・行の重なり・fit に使った行・目的変数・識別子・
    同じ患者が両側に出ていないか、を表にする。**通ったことの証拠として残せる。**
  - 出力は**列名付き DataFrame**（`set_output("pandas")` ＋
    `verbose_feature_names_out=False`）。`欠損あり_年齢`、`施設=B院` のように読める名前にする。
  - one-hot は既定で基準水準を落とし、`reference_levels()` でどれを基準にしたかを出す。
  - **日付列は既定で特徴量に入れない**（暦の効果を学習させないため）。
  - `distribution_shift()` — train の範囲外に出る値の割合。
    **train 自身の丸め率と比べる**ので、もともと歪んだ列で誤報しない。
- `splitting` — **分割。リークはここで始まる。**
  - **同一 ID に重複があれば自動でグループ単位に分ける**（同じ患者が両側に入るのを防ぐ）。
  - 分類は目的変数で、生存時間はイベントで層化する。
  - `time_order=` で**時間順分割**（過去で学習し未来で検証する）。
  - train と test の分布を SMD で比較し、|SMD| ≥ 0.2 の列を警告する。
  - `cv_splitter()` / `fold_summary()`。グループが両側に出ていないことを表で確かめられる。
- `missing` — **欠損の分析。補完はしない**（補完は train でしか fit できないので pipeline の仕事）。
  - 列別・症例別・パターン別の欠損、`mcar_signals()` による**欠損の偏りの名指し**、
    `drop_missing_outcome()`（目的変数は補完してはならない）、missingno による可視化。
- `outliers` — **外れ値と入力ミスを区別する。**
  - `NanSafeWinsorizer` — NaN を素通しする sklearn 変換器。
    feature-engine の `Winsorizer` は NaN で停止するため自作した。
    **閾値は fit でしか決めない。**
  - IQR / MAD / 分位点。**MAD が 0 の列では閾値を作らない**（0 で割ると全例が外れ値になる）。
  - 群ごとの閾値、Mahalanobis 距離、IsolationForest。
  - `drop` は人が明示したときだけ。**医学では外れ値こそが重要な症例でありうる。**

### 修正（Phase 9）
- `clean.derive()` が **Alb 欠測時に「補正されていない Ca」を補正Ca として出していた。**
  `np.nan < 4.0` が `False` に落ちるためで、合成データ 600 例では 49 例が該当した。
  管理目標の達成率も Cox の係数も静かにずれる。算出不能として NaN にするよう直した。
  **見つけたのは `missing.mcar_signals()`**（「Alb の欠測と補正Ca が関連している」）。
- `outliers.detect` の「歪んだ分布」の注記を 1 割から 5% に下げた
  （正規分布なら IQR 法で外れるのは 0.5% ほど）。

### 追加（Phase 8）
- `survival` — **Kaplan-Meier・log-rank・Cox 回帰・比例ハザードの検定。**
  `survival_input` が「日付から (duration, event) を作る」ところを担い、
  ここはその先を担う。`Survival.from_survival_frame(sf)` でつながる。
  - `km(by=...)` — 生存曲線、at-risk 表、生存期間中央値（95%CI）、1年/3年生存率、
    **追跡期間中央値（reverse Kaplan-Meier）**。中央値に到達しなければ **NR** と書く
    （NaN で流さない）。信頼区間の帯は 2 群までを既定にし、at-risk 表は
    「At risk」の行だけ出す（lifelines の既定 3 行だと 4 群で曲線が潰れる）。
  - `logrank(by=...)` — 2群は log-rank、3群以上は多群 log-rank ＋ **対比較（Holm 補正）**。
    順序のある群には **log-rank trend test**（lifelines に無いので自前実装。
    2群でスコア (0,1) を与えると通常の log-rank と一致することを回帰テストで固定）。
  - `cox(covariates="auto")` — 単変量スクリーニング（p < 0.10）→ 多変量。
    **逐次選択（stepwise）は使わない**（選択後推論の問題）。
    カテゴリは自動でダミー化し、**基準水準を必ず記録する**。
    定数・分散 0・水準過多の共変量は理由つきで外す。
  - **★ Cox の「黙って減る n」を必ず報告する ★**
    共変量に欠測があると `CoxPHFitter` は完全ケースだけで推定する。
    何例・何イベントが落ちたか、どの列の欠測が多いかを警告として返す。
  - EPV < 10 の警告、信頼区間が極端に広い変数（完全分離の兆候）の警告、
    lifelines の `ConvergenceWarning` を握り潰さず結果に取り込む。
  - `check_ph()` — Schoenfeld 残差による検定。違反があれば
    **どうすればよいか**（層別化 / 時間依存項 / RMST）を名前で挙げる。
  - `rmst(by=..., t=...)` — 制限付き平均生存時間。**比例ハザードが成り立たないときの逃げ道。**
  - `forest()` — ハザード比のフォレストプロット（対数軸を素の数で目盛る）。
  - `schoenfeld_plot()` — 残差の図。「違反」と言われたらまず形を見る。

### 追加（Phase 7）
- `describe` — **記述統計・Table 1・群間比較・管理目標の達成率。**
  表を出すだけでなく、**どの検定をなぜ選んだか**を「判定の根拠」列に残す。
  - `table_one(df, schema, groupby=...)` — 連続変数は正規性の判定に従って
    平均(SD) / 中央値[Q1,Q3] を書き分け、カテゴリは水準ごとに n(%)。
    項目名に**単位と採血時点**を出す（透析前 BUN と透析後 BUN が同じ表に並ぶため）。
    `to_excel()` / `to_html()` / `report()`。
  - `compare_groups(df, by, ...)` — 検定の自動選択。
    2群正規 → Welch の t／2群非正規 → Mann-Whitney U／3群以上正規 → 一元配置分散分析
    →（事後）Tukey HSD／3群以上非正規 → Kruskal-Wallis →（事後）Dunn（Holm 補正）／
    カテゴリ → χ²（期待度数 < 5 が 2 割超なら Fisher 正確検定に自動切替）／
    `paired_by=` で対応のある t・Wilcoxon 符号付順位。
  - **効果量を必ず併記**（Hedges' g / Cliff's δ / η² / ε² / Cramér's V）と SMD。
    3 群以上の SMD は対ごとの最大値。多水準の SMD は Yang & Dalton 法。
  - 列数ぶんの p 値には **BH の q 値を併記**する。
  - **ベースライン表の p 値の解釈上の注意を必ず添える**
    （無作為割付なら意味が無く、観察研究なら例数で小さくなる。バランスは SMD で見る）。
  - `target_achievement(df, by=...)` — 辞書から列の対応を自動で作り、管理目標の達成率を出す。
    境界値ちょうどの症例数を必ず併記する。
  - `normality(x)` — 平均(SD) で要約してよいかの判定。**検定の棄却だけでは非正規としない**
    （大標本では実用上どうでもよい歪みでも棄却されるため）。歪度・尖度と併用し、
    どちらの基準で決めたかを根拠として残す。

### 追加
- `schema` — **列の役割を推定し、その判断を理由つきで書き出す。**
  役割は id / outcome / time / event / group / datetime / numeric / binary /
  ordinal / nominal / high_cardinality / text / constant / duplicate_of / unknown。
  `Schema.to_yaml()` で `schema.yaml` として保存し、人が直して再実行できる。
  `diff()` が手で直した箇所を表で返す。**`reason` の無い判断を作らない。**
  - 日付の判定をユニーク率による ID 判定より先に行う（逆だと観察開始日が ID にされる）。
  - ユニーク率だけで識別子と断定しない（自由記載もユニーク率 1.00 になる）。
    「識別子ではないか」の問いかけは `audit` が行う。
  - 数値列は列名の裏づけが無ければ順序尺度とみなさない
    （施設コード {1,2,3,4} が ECOG {0,1,2,3,4} の部分集合になってしまう）。
  - 重複列の検出は型をまたぐ。施設（文字列）と施設コード（整数）は
    Cramér's V で捕まえる。
- `quality` — **データ品質監査。「このまま解析してよいか」を問う。**
  公開する関数は `audit()`。**audit は直さない。報告する。**
  所見には必ず `action`（どうすればよいか）を付ける。
  - 重複行・同一 ID の複数行（Cox もロジスティック回帰も 1 人 1 行を仮定している）
  - **透析前後の取り違えの検出**。BUN は透析で必ず下がる。下がらない症例が
    過半数なら病態ではなく列の取り違えである。取り違えたまま spKt/V を計算すると
    透析量が過大に評価され、どの値も単独では正常範囲に見える。
  - **測定法変更の段差検出**（`method_change_steps`）。ALP の 2020-04 JSCC→IFCC で
    値がおよそ 1/3 になる。これを病態として解析すると存在しない所見が出る。
  - 白血球分画の検算（好中球を使い、桿状核球・分葉核球は合計に含めない）、
    桿状核球＋分葉核球 ≒ 好中球 の照合
  - Fe > TIBC（TSAT>100%）、収縮期血圧 ≤ 拡張期血圧、Payne 式と合わない補正Ca、
    身長・体重からあり得ない BMI
  - 生存時間の矛盾（イベント日と打ち切り日の両方／どちらも無い／逆転／0 日）
  - 目的変数（単一クラス・極端なクラス不均衡・欠測）と**リークの検出**
  - 群サイズ、欠測率、**欠測が群に偏っていないか**（MCAR でなければ
    全体の中央値による補完が群間差を人工的に作る）
  - 日付の並びが確定できない列、未来の日付、解釈できない値
  - 表記ゆれ（正規化すると同じになる値が別の水準として入っている）

### 修正
- `schema` の数値判定が緩すぎた。「数字以外を落として数値化できるか」で
  判定していたため、`特記なし_12` のような自由記載が 12 として数値列にされていた。
  **数値は先頭にあること**、後ろに続くのは数字を含まない短い単位であることを要求する。
- `schema` がユニーク率 > 0.90 の列を無条件に落としていた。年齢・Cr のような
  連続値は症例数が少なければユニーク率 1.00 になるため、
  **いちばん大事な説明変数から先に消えていた**。この規則は数値列には適用しない。

### 変更
- 合成データ（`examples/make_synthetic.py`）に `検体採取日` を追加し、登録期間を
  2011〜2020 に広げた。従来は全症例が 2020-04 より前に収まっており、
  docstring が謳っていた **ALP の測定法変更の段差が実際には入っていなかった**。
- `examples/run_e2e.py` に schema と audit の 2 段を追加した。

### 変更（CI）
- `actions/checkout` を v4 → v7、`astral-sh/setup-uv` を v5 → v10.1.0 に上げた。
  setup-uv はリリースが immutable で浮動の major タグ（`v10`）を作っていないため、
  実在するタグに厳密固定する。
  v4 / v5 は Node.js 20 を指しており、実行環境が強制的に Node 24 に載せ替えていた。
- `cache-dependency-glob: "pyproject.toml"` を指定した。ライブラリなので `uv.lock` を
  追跡しておらず、既定のパターンに何も一致せずキャッシュ鍵が固定されていた。
  **一度作ったキャッシュが永久に無効化されない**状態だった。
- 週次実行（毎週月曜 09:00 JST）を追加した。ロックファイルを持たない以上、依存は
  毎回新しく解決される。「上流の新版で壊れた」は push の無い週にも起こるので、
  定期的に回して気づけるようにした。
- ワークフローの既定権限を `contents: read` に絞った。
- `cache-suffix` をジョブごとに分けた。setup-uv の既定のキャッシュ鍵には matrix の
  Python 版が入らないため、同一 OS の 3 セルが同じ鍵を同時に取り合い
  `Failed to save: Unable to reserve cache with key ...` を毎回出していた。
  cp311 と cp313 では必要な wheel も違うので、セルごとに分けるほうが正しい。

### 予定
- `autoprep()` — 全自動 1 行の結線

## [0.1.0] - 2026-09-18

最初の公開。前処理の土台と、透析領域に固有の計算を実装した。

### 追加
- `dates` — 多様な日付表記の正規化。`2013/3/3` `{2013, 3, 3}` `2013.3.3` `2013,3,3`
  `2013-3-3 14:32:11`（時刻は自動削除）、和暦（`H25.3.3` `令和元年5月1日`）、
  Excel シリアル値、全角、`20130303` に対応。
  **日と月の順序が列内から確定できない場合は解釈せず、指定を求める。**
- `survival_input` — 生存時間データの 3 形式を `(duration, event)` に正規化。
  - 形式A `duration` + `event`
  - 形式B `start_date` + `end_date` + `event`
  - **形式C `id` + `start_date` + `event_date` + `censor_date`（日付だけで完結）**
  イベント日と打ち切り日の両方が入っている矛盾、どちらも空欄、日付の逆転、
  未来日付、観察期間 0 を検出し、除外理由を症例ごとに報告する。
- `clean` — 医学領域辞書に基づく掃除。検出限界（`<0.1` → 1/2 LOD ＋ 打切りフラグ）、
  欠損コード（分布から浮いた値のみ）、単位混在（**あり得ない範囲の外の値だけ**換算）、
  生理学的にあり得ない値の NaN 化（外れ値処理ではなく入力ミスの検出）。
- `timing` — 採血時点（透析前／透析後／不詳）。列名からの自動判定と縦持ち対応。
  **不詳の値から透析指標を計算しない。**
- `hd` — 透析指標。URR、spKt/V（Daugirdas 第2世代）、nPCR（Kaynar 2012）、
  **%CGR（11ステップ）**、クリアスペース率 A/V（実測法・推算法）、推定食塩摂取量、
  GNRI（ドライウェイト優先）、補正Ca（Payne 式）、TSAT、BMI。
- `tac` — TAC-BUN。簡便式（週初め透析後＋次回透析前）、台形則、時間加重近似、
  尿素との換算。**取り違え（同一回の前後の平均、引数の順序）を自動検出する。**
- `targets` — 管理目標の達成判定。**開区間と閉区間を区別する**
  （「5.5 未満」に 5.5 は含まれない）。境界値ちょうどの症例数を必ず報告する。
- `dict/ranges_ja.yaml` — 86 項目の医学領域辞書。
  表記ゆれ、単位、生理学的にあり得ない範囲、基準範囲、JSDT 管理目標（出典つき）、
  測定法の変更（ALP の JSCC→IFCC 等）、欠損コード、検出限界。

### 検証
- %CGR の 12 個の中間値が、提示された計算例と 1e-11 以内で一致。
- クリアスペース率 A/V が計算例（15120 mg-N, 60 mg/dL, 36 L → 70.0%）と完全一致。
- TAC 簡便式（20, 60 → 40 mg/dL）と換算（→ 尿素 85.7 mg/dL, 14.3 mmol/L）が一致。
- 合成コホート 600 例で、仕込んだ汚れ 7 種の検出件数が完全一致。
- 真の係数が既知の合成データで、Cox の 95%CI が 6 変数中 5 変数で真値を含む。
