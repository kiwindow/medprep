"""第3回「データの前処理」スライド ver1.0 を作る。

高さは目分量で置かない。すべて本文の量から計算し、積み上げた戻り値で次を置く。
"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from deck import (  # noqa: E402
    BLUE,
    CYAN,
    GREEN,
    INK,
    LIGHT,
    MINT,
    NAVY,
    PALE,
    PALEB,
    RED,
    SKY,
    WHITE,
    Deck,
    cover,
)

D = Deck()
TOP = 1.80          # 本文の始まり
BOT = 6.95          # ここより下に置かない
FULL = 12.23        # 本文の幅
L = 0.55

# ============================================================ 1 表紙
cover(D, "日本腎・血液浄化AI学会  演習講座「鹿鳴館」  第1期 第3回",
      ["データの前処理", "―― 解析を始める前の仕事"],
      "医療データ特有の落とし穴を、見つけて・記録に残して・再現する",
      ["medprep", "Colab / Win / Mac", "Python 3.12", "3時間"],
      "対象：第1回・第2回で環境を作り終えた みなさん")

# ============================================================ 2 ゴール
s = D.slide("この回のゴール", "持ち帰るのは、道具ではなく判断の順序")
y = D.card(s, L, TOP, 4.0, None, "汚れを見つける",
           ["欠損コード・単位混在・",
            "測定法の変更・打切り。",
            "**検定でも例外でも捕まらない**",
            "ものを見つける手順。"], badge="1", bs=14)
D.card(s, 4.72, TOP, 4.0, None, "判断を記録に残す",
       ["機械の判断には必ず理由が付く。",
        "読んで、納得できなければ直す。",
        "**schema.yaml が**",
        "**再現性の中核**になる。"], badge="2", bs=14)
D.card(s, 8.89, TOP, 3.89, None, "リークを防ぐ",
       ["前処理は train だけで fit する。",
        "気をつける話ではなく、",
        "**できない構造**にする。"], badge="3", fill=NAVY,
       head_color=SKY, body_color=PALEB, badge_fill=CYAN, badge_color=INK, bs=14)
y = D.callout(s, [[("全自動とは「決定の自動化」ではなく、", PALEB),
                   ("「決定の明示化」である。", WHITE, 17)]], L, y + 0.40, FULL)
D.text(s, ["1 行で最後まで走る。しかし**そこで下した判断はすべて外に出る**。",
           "「この列は ID とみなした」「この値は欠損コードとみなした」「この症例は除外した」"
           "——その理由が残り、人が直して再実行できる。"],
       L, y + 0.30, FULL, 1.0, 15, INK)

# ============================================================ 3 コマ割り
s = D.slide("3 時間の流れ", "前半は「見つける」、後半は「渡す」")
rows = [
    ["0:00–0:20", "なぜ前処理が仕事の 8 割なのか／医療データ特有の落とし穴", "スライド"],
    ["0:20–0:35", "まず全自動で走らせる  mp.autoprep(...) 1 行 → レポートを開く", "演習①"],
    ["0:35–1:00", "レポートを読む。schema.yaml で機械の判断を検分する", "演習②"],
    ["1:00–1:25", "欠損と外れ値。消してよい場合と消してはいけない場合", "演習③"],
    ["1:25–1:35", "休憩", ""],
    ["1:35–2:05", "Table 1 と群間比較。検定の自動選択、効果量、p 値の罠", "演習④"],
    ["2:05–2:35", "生存時間分析。KM → log-rank → Cox → 比例ハザードの確認", "演習⑤"],
    ["2:35–2:50", "目的変数を作り、モデルに渡す。リークが起きない理由", "演習⑥"],
    ["2:50–3:00", "発展事項（MICE・競合リスク・時系列）と第4回の予告", "スライド"],
]
y = D.table(s, ["時間", "内容", "形式"], rows, L, TOP, [1.55, 9.10, 1.58], rh=0.46,
            colors={4: MINT})
D.note(s, "演習は 1 つのノートブック Preprocessing_Ver1_0.ipynb を上から順に実行するだけ。"
          "自分のデータが無い方には、汚れを仕込んだ合成透析コホート（600 例）を用意してある。",
       L, y + 0.25, FULL, 0.60)

# ============================================================ 4 なぜ8割
s = D.slide("なぜ前処理が仕事の 8 割なのか", "モデルを替えても、入力が歪んでいれば結論は歪む")
y = D.card(s, L, TOP, 5.95, None, "統計解析のデータは「きれい」である",
           ["論文になった表は、誰かが数週間かけて整えた後の姿である。",
            "整える過程は論文に書かれない。だから学ぶ機会が無い。"], fill=PALE)
D.card(s, 6.83, TOP, 5.95, None, "診療のデータは「記録」である",
       ["記録は解析のために書かれていない。単位も表記も測定法も、",
        "その時々の都合で変わる。**それが正しい記録のあり方**でもある。"], fill=PALE)
y = D.callout(s, [[("モデルの選択で変わるのは数 % 。", PALEB),
                   ("前処理の誤りで変わるのは結論そのもの。", WHITE, 17)]],
              L, y + 0.35, FULL)
D.text(s, [[("たとえば ――", INK, 15)],
           "・ 欠損を 0 で埋めた  → 「検査していない」が「値が 0」になり、平均が下がる",
           "・ <0.1 を捨てた  → 炎症の無い症例だけが落ち、CRP の分布が高い側に寄る",
           "・ 測定法の変更を見落とした  → 「2020 年以降 ALP が下がった」という存在しない所見",
           "・ 前処理してから分割した  → test の情報が混ざり、性能が実際より良く見える"],
       L, y + 0.30, FULL, 1.30, 14, INK, line=1.34)

# ============================================================ 5 落とし穴
s = D.slide("医療データ特有の落とし穴", "どれも例外を出さずに通ってしまう")
items = [
    ("欠損コード", ["999 / 「未測定」 / 「−」。", "数値として平均に混ざる"]),
    ("単位の混在", ["Hb を g/dL と g/L で記録。", "10 倍の値が同じ列に並ぶ"]),
    ("検出限界", ["<0.1 は数値ではない。", "捨てると分布が偏る"]),
    ("日付の表記", ["和暦・全角・Excel シリアル値・", "時刻付きが 1 列に混在"]),
    ("測定法の変更", ["ALP の JSCC→IFCC。", "値が 1/3 になるが病態は不変"]),
    ("打切りの扱い", ["イベント日と打切り日の", "両方が入っている症例がある"]),
]
bottom = TOP
for i, (h, b) in enumerate(items):
    yy = D.card(s, L + (i % 3) * 4.17, TOP + (i // 3) * 1.80, 4.0, 1.62, h, b,
                fill=LIGHT, hs=17, bs=14)
    bottom = max(bottom, yy)
y = D.callout(s, [[("これらは「汚い」のではない。", PALEB),
                   ("記録として正しく、解析には向かない", WHITE, 17),
                   ("、というだけである。", PALEB)]], L, bottom + 0.30, FULL)
D.note(s, "だから「直す」のではなく、まず「見つけて、どう扱ったかを記録に残す」。",
       L, y + 0.22, FULL, 0.40)

# ============================================================ 6 明示化
s = D.slide("この回の中心にある考え方", "全自動は「決定の自動化」ではない")
D.box(s, L, TOP, 5.95, 3.45, PALE)
D.text(s, "✗  決定の自動化（してはいけない）", 0.87, TOP + 0.33, 5.3, 0.4, 18, RED)
D.text(s, ["・ 欠損を黙って中央値で埋める",
           "・ 外れ値を黙って削除する",
           "・ 「数値でない値」を黙って落とす",
           "・ 何をしたか、結果から復元できない",
           "",
           "→ 誰も検分できず、**誤りに気づけない**"],
       0.87, TOP + 0.95, 5.3, 2.3, 15, INK, line=1.42)
D.box(s, 6.83, TOP, 5.95, 3.45, NAVY)
D.text(s, "✓  決定の明示化（これをする）", 7.15, TOP + 0.33, 5.3, 0.4, 18, SKY)
D.text(s, ["・ 判断には必ず理由を付ける",
           "・ 判断を schema.yaml に書き出す",
           "・ 人が直して再実行できる",
           "・ 確認が要る事項を先頭にまとめる",
           "",
           "→ **人の判断が要る場所が見える**"],
       7.15, TOP + 0.95, 5.3, 2.3, 15, PALEB, line=1.42)
D.callout(s, [[("確認事項が空で返ってきたら、まずそちらを疑うこと。", WHITE, 17),
               ("  医学データで所見ゼロはまず無い。", PALEB)]],
          L, TOP + 3.80, FULL, fill=BLUE, color=WHITE)

# ============================================================ 7 演習1
s = D.slide("演習①  0:20–0:35", "まず全自動で走らせる")
y1 = D.code(s, ["rep = mp.autoprep(",
                "    df,",
                "    group='施設', id_col='仮名ID',",
                "    date_col='検体採取日',",
                "    survival_dates=SURVIVAL_DATES,",
                "    save=True,   # run{N}/ に全出力を保存",
                ")"], L, TOP, 6.10, size=13)
y2 = D.card(s, 7.00, TOP, 5.78, None, "返ってくるもの",
            ["rep.X_train / rep.y_train  学習に渡せる行列",
             "rep.pipeline  fit 済みの前処理",
             "rep.schema  列の役割と、その理由",
             "rep.warnings  人の確認が要る事項",
             "rep.html  HTML 1 枚のレポート"], fill=PALE, bs=14)
y = max(y1, y2) + 0.32
D.text(s, "出力（段ごとの成否が全部残る）", L, y, 6.0, 0.35, 17, BLUE)
y3 = D.code(s, ["・ 列の役割を推定する  20 列（うち外す列 3 本）",
                "・ 品質を監査する    致命的 5 / 要確認 1 件",
                "・ リークを検査する   6 項目すべて OK",
                "・ run に保存する    run1"], L, y + 0.42, 7.40, size=12.5)
D.card(s, 8.15, y + 0.42, 4.63, None, "止まるところ",
       ["**背骨**（読む・監査・分割・前処理）で",
        "失敗したら止まる。**付随するもの**",
        "（管理目標・生存時間・図）は飛ばす。"],
       fill=MINT, head_color=GREEN, hs=17, bs=14)

# ============================================================ 8 レポート
s = D.slide("レポートは警告から始まる", "10 枚目にある致命的所見は、読まれないのと同じ")
y = D.code(s, ["  5 ✗ 致命的 — このまま解析すると誤った結論になる",
               "  1 ! 要確認        3 · 記録",
               "",
               "まず読むこと",
               "  ✗ 致命的 測定法  'ALP' は 2020-04-01 の前後で中央値が",
               "           246 → 91（0.37 倍、p=4.2e-33）と段差になっている",
               "           → 期間を共変量に入れるか、期間で層別すること",
               "  ✗ 致命的 生存時間  イベント日と打ち切り日の両方が入っている"],
            L, TOP, FULL, size=13)
yy = y + 0.35
D.card(s, L, yy, 4.0, None, "HTML 1 枚",
       ["図は埋め込み。そのままメールに乗る。",
        "別ファイルにすると、送った時点でばらける。"], fill=PALE, hs=17, bs=14)
D.card(s, 4.72, yy, 4.0, None, "症例レベルの値は出さない",
       ["既定では件数だけ。**仮名 ID でも症例を特定しうる**。",
        "show_values=True を明示したときだけ出る。"], fill=PALE, hs=17, bs=14)
D.card(s, 8.89, yy, 3.89, None, "図には必ず表を添える",
       ["色だけに頼らせない。",
        "色覚特性のある読者でも同じ結論に届く。"], fill=PALE, hs=17, bs=14)

# ============================================================ 9 schema
s = D.slide("演習②  0:35–1:00", "機械の判断を検分する ―― ここが山場")
rows = [
    ["仮名ID", "id", "drop", "人が id_col として指定した"],
    ["施設コード", "duplicate_of", "drop", "'施設' と Cramér's V = 1.0000（実質同じ情報）"],
    ["備考", "high_cardinality", "drop", "ユニーク率 1.00 > 0.90（ほぼ全行で異なる）"],
    ["検体採取日", "datetime", "keep", "100% の値を日付として解釈できた（並び: ymd）"],
    ["CRP 定量", "numeric", "keep", "記号・単位の除去で 100% が数値化できる"],
]
y = D.table(s, ["列名", "役割", "扱い", "判断の根拠"], rows, L, TOP,
            [2.40, 2.20, 1.00, 6.63], rh=0.43)
y = D.callout(s, [[("reason の無い判断は作らない。", WHITE, 17),
                   ("  推定できなかった列は unknown にして人に返す。", PALEB)]],
              L, y + 0.24, FULL)
D.text(s, ["**確かめること**",
           "・ drop になった列は、本当に捨ててよいか",
           "・ numeric の列に、数値でないものが混ざっていないか",
           "・ datetime の列が、日と月を取り違えていないか"],
       L, y + 0.24, 6.2, 1.3, 14, INK, line=1.32)
D.code(s, ["# 直したいときは編集して読み直す",
           "sch = mp.Schema.from_yaml('schema.yaml')"],
       6.95, y + 0.24, 5.83, size=12.5)

# ============================================================ 10 測定法
s = D.slide("見つけにくい汚れの代表", "測定法の変更 ―― 検定でも例外でも捕まらない")
y1 = D.card(s, L, TOP, 5.95, None, "2020 年 4 月  ALP の測定法が変わった",
            ["JSCC 法 → IFCC 法。日本中の検査室で同時に切り替わった。",
             "**値はおよそ 1/3 になるが、患者の病態は何も変わっていない。**",
             "240 U/L だった人が 80 U/L と記録される。"], fill=LIGHT)
D.box(s, 6.83, TOP, 5.95, y1 - TOP, NAVY)
D.text(s, "見落とすとこうなる", 7.15, TOP + 0.33, 5.3, 0.4, 18, SKY)
D.text(s, ["「2020 年以降、当院の患者は ALP が下がった」",
           "「新しい治療が奏効した」",
           "",
           "→ **存在しない所見が論文になる**"],
       7.15, TOP + 0.95, 5.3, 1.3, 15, PALEB, line=1.42)
y = y1 + 0.32
D.text(s, "日付で切って中央値を比べて初めて見える", L, y, 8.0, 0.35, 17, BLUE)
y2 = D.code(s, ["steps = mp.method_change_steps(df, date_col='検体採取日')",
                "",
                "✗ 致命的 'ALP' は 2020-04-01 の前後で中央値が 246 → 91",
                "         （0.37 倍、p=4.2e-33、前 539 例 / 後 61 例）",
                "# IFCC ≒ JSCC × 0.35 の近似換算は可。厳密換算は不可"],
            L, y + 0.40, 8.10, size=12)
y3 = D.card(s, 8.85, y + 0.40, 3.93, None, "他にもある",
            ["・ Alb の BCG → BCP 改良法",
             "・ HbA1c の JDS → NGSP",
             "・ 切替日が施設ごとに違うもの"], fill=PALE, hs=17, bs=13)
D.note(s, "期間を共変量に入れるか、期間で層別すること。",
       L, max(y2, y3) + 0.16, FULL, 0.4)

# ============================================================ 11 欠損
s = D.slide("演習③  1:00–1:25", "欠損 ―― 「測っていない」ことが情報である")
y1 = D.card(s, L, TOP, 5.95, None, "なぜ 0 で埋めてはいけないか",
            ["重症だから測った／軽症だから測らなかった、",
             "という欠損は予後と結びついている。",
             "黙って中央値で埋めると、**その情報を捨てたうえに分布を歪める**。"],
            fill=PALE)
D.card(s, 6.83, TOP, 5.95, y1 - TOP, "だから既定で指示変数を足す",
       ["「この値は欠けていた」という列を残す。",
        "**欠損そのものが予後因子でありうる**。",
        "医学では珍しいことではない。"], fill=PALE)
y = y1 + 0.32
D.text(s, "欠損は偏っているか（MCAR か）", L, y, 8.0, 0.35, 17, BLUE)
D.text(s, ["欠損が完全にランダムなら、欠損フラグはどの列とも関連しないはずである。",
           "関連があれば単純補完は分布を歪める。"
           "**どの列の欠損がどの列と結びついているかを名指しする。**"],
       L, y + 0.40, FULL, 0.8, 14, INK, line=1.35)
D.code(s, ["rep.mcar   # 欠損フラグと他の列の総当たり検定",
           "  欠損のある列     関連する列      p",
           "  アルブミン(Alb)   施設          2.1e-08",
           "  補正Ca          アルブミン(Alb)  3.4e-31 ← Alb から計算される"],
       L, y + 1.05, FULL, size=12)

# ============================================================ 12 外れ値
s = D.slide("演習③（続き）", "外れ値 ―― 消してよい場合と消してはいけない場合")
rows = [
    ["入力ミス", "Hb 0 g/dL、年齢 250 歳、体重 3.5 kg の成人",
     "生理学的にあり得ない。NaN にする", "辞書"],
    ["本物の外れ値", "Alb 1.8 g/dL、CRP 25 mg/dL、iPTH 2000 pg/mL",
     "★消してはならない★　重要な症例でありうる", "残す"],
    ["単位の取り違え", "Hb 108（g/L で記録）", "換算できる。換算したことを記録に残す", "辞書"],
]
y = D.table(s, ["種類", "例", "扱い", "判断"], rows, L, TOP,
            [2.00, 4.30, 4.53, 1.40], rh=0.54)
y = D.callout(s, [[("この区別は統計では付かない。", WHITE, 17),
                   ("  医学領域辞書（86 項目）が担う。", PALEB)]], L, y + 0.20, FULL)
D.card(s, L, y + 0.20, 5.95, None, "medprep は外れ値を削除しない",
       ["既定は検出して報告するだけ。winsorize するときも、",
        "**閾値は train だけで決める**。"], fill=PALE, hs=17, bs=14)
D.card(s, 6.83, y + 0.20, 5.95, None, "医学では外れ値が主役のことがある",
       ["劇症型、稀な合併症、想定外の反応。",
        "**外れ値を消すと、見たい症例が消える。**"],
       fill=MINT, head_color=GREEN, hs=17, bs=14)

# ============================================================ 13 Table1
s = D.slide("演習④  1:35–2:05", "Table 1 ―― 検定は自動で選ぶ。ただし理由を必ず添える")
rows = [
    ["2 群・正規", "Welch の t 検定", "Hedges' g", "等分散を仮定しない"],
    ["2 群・非正規", "Mann-Whitney U 検定", "Cliff's δ", "歪度が大きい／外れ値がある"],
    ["3 群以上・正規", "一元配置分散分析 ＋ Tukey HSD", "η²", "事後比較まで出す"],
    ["3 群以上・非正規", "Kruskal-Wallis 検定 ＋ Dunn 法", "ε²", "事後比較まで出す"],
    ["カテゴリ", "χ² 検定／Fisher 正確検定", "Cramér's V", "期待度数 5 未満なら Fisher"],
]
y = D.table(s, ["データ", "選ばれる検定", "効果量", "選んだ理由"], rows, L, TOP,
            [2.35, 4.30, 1.90, 3.68], rh=0.50)
y = D.callout(s, [[("検定名だけでなく「なぜそれを選んだか」を表に残す。", WHITE, 17),
                   ("  査読で必ず聞かれる。", PALEB)]], L, y + 0.30, FULL)
D.text(s, ["**多重比較の補正を忘れない** ―― Table 1 は全列に p を出す。"
           "20 列あれば、帰無仮説がすべて正しくても 1 つは p<0.05 になる。",
           "medprep は Benjamini-Hochberg の q 値を必ず併記する。"],
       L, y + 0.32, FULL, 1.0, 15, INK, line=1.45)

# ============================================================ 14 p vs SMD
s = D.slide("演習④（続き）", "群間の「偏り」は p ではなく SMD で見る")
H = 3.05
D.box(s, L, TOP, 5.95, H, PALE)
D.text(s, "p 値は例数で動く", 0.87, TOP + 0.33, 5.3, 0.4, 18, RED)
D.text(s, ["・ n=50 なら「差が無い」",
           "・ n=5000 なら同じ差が「有意」",
           "",
           "**偏りの大きさは変わっていない。**",
           "変わったのは例数だけである。"],
       0.87, TOP + 0.95, 5.3, 2.0, 15, INK, line=1.42)
D.box(s, 6.83, TOP, 5.95, H, NAVY)
D.text(s, "SMD は例数で動かない", 7.15, TOP + 0.33, 5.3, 0.4, 18, SKY)
D.text(s, ["標準化平均差（standardized mean difference）",
           "",
           "|SMD| < 0.1  → 釣り合っている",
           "|SMD| ≧ 0.2  → 偏っている",
           "",
           "RCT の基線表では p の掲載自体が推奨されない。"],
       7.15, TOP + 0.95, 5.3, 2.2, 15, PALEB, line=1.38)
y = D.callout(s, [[("train と test の比較も同じ。", WHITE, 17),
                   ("  |SMD| ≧ 0.2 の列があれば、性能の差は"
                    "「モデルの差」ではなく「分割の運」かもしれない。", PALEB)]],
              L, TOP + H + 0.35, FULL, fill=BLUE, color=WHITE)
D.note(s, "medprep は Table 1 にも、train/test のバランス表にも SMD を必ず併記する。",
       L, y + 0.22, FULL, 0.4)

# ============================================================ 15 管理目標
s = D.slide("演習④（続き）", "管理目標 ―― 開区間と閉区間で達成率が動く")
D.text(s, ["JSDT の管理目標には、**境界値を含むものと含まないものがある**。"
           "`<` と `<=` の取り違えは、達成率を数 % 動かす。"],
       L, TOP, FULL, 0.5, 16, INK)
rows = [
    ["無機リン (P)", "3.5 ≦ P < 6.0", "46.2%", "49.7%", "+3.5%（21 例）"],
    ["補正Ca", "8.4 ≦ cCa < 10.0", "42.8%", "49.0%", "+6.2%（34 例）"],
    ["Hb", "10 ≦ Hb < 12", "59.0%", "61.8%", "+2.8%（17 例）"],
]
y = D.table(s, ["項目", "管理目標（正しい区間）", "正しく開区間", "誤って閉区間", "差"],
            rows, L, TOP + 0.46, [2.45, 3.25, 2.05, 2.05, 2.43], rh=0.48)
y = D.callout(s, [[("補正Ca では達成率が 6.2 ポイント動く。", WHITE, 17),
                   ("  論文の結論が変わる大きさである。", PALEB)]], L, y + 0.24, FULL)
D.card(s, L, y + 0.18, 5.95, None, "なぜ間違えるのか",
       ["四捨五入された値ほど、境界値ちょうどに集まる。"],
       fill=PALE, hs=17, bs=14)
D.card(s, 6.83, y + 0.18, 5.95, None, "medprep の扱い",
       ["区間の開閉を辞書に持たせ、境界値の例数を出す。"],
       fill=PALE, hs=17, bs=14)

# ============================================================ 16 生存
s = D.slide("演習⑤  2:05–2:35", "生存時間 ―― 黙って減る n を見張る")
y1 = D.card(s, L, TOP, 5.95, None, "4 列の日付から (duration, event) を作る",
            ["ID ／ 観察開始日 ／ イベント発生日 ／ 打ち切り日。",
             "**矛盾した症例は勝手に解釈しない。**",
             "除外して、理由を表に残す（今回は 600 → 578 例）。"], fill=PALE)
D.box(s, 6.83, TOP, 5.95, y1 - TOP, NAVY)
D.text(s, "Cox は黙って症例を落とす", 7.15, TOP + 0.33, 5.3, 0.4, 18, SKY)
D.text(s, ["共変量に 1 つでも欠測があれば、その症例は",
           "推定に入らない。578 例で始めたはずが",
           "508 例で推定されている。**どこにも表示されない。**"],
       7.15, TOP + 0.95, 5.3, 1.4, 15, PALEB, line=1.42)
y = y1 + 0.32
y2 = D.code(s, ["Cox 比例ハザード回帰  n = 508、イベント = 203、EPV = 33.8",
                "欠測により除外: 70 例（うちイベント 24 件）  ← ★必ず報告する★",
                "",
                "           HR    95%CI下限  95%CI上限        p",
                "年齢     1.0384    1.0259    1.0511   0.0000",
                "Alb      0.5192    0.3787    0.7118   0.0000"], L, y, 7.90, size=13)
D.card(s, 8.65, y, 4.13, None, "EPV を見る",
       ["イベント数 ÷ 共変量の数。",
        "**10 を下回れば推定が不安定**。",
        "共変量を減らすか、例数を増やす。"],
       fill=MINT, head_color=GREEN, hs=17, bs=14)
D.note(s, "比例ハザード仮定は Schoenfeld 残差で検定する。有意なら層別化するか、"
          "比例ハザードを仮定しない RMST を使う。合成データなら真の係数と照合できる。",
       L, y2 + 0.22, FULL, 0.6)

# ============================================================ 17 リーク
s = D.slide("演習⑥  2:35–2:50", "リークを構造的に不可能にする")
H = 2.62
D.box(s, L, TOP, 5.95, H, PALE)
D.text(s, "✗ よくある手順（リークする）", 0.87, TOP + 0.28, 5.3, 0.4, 18, RED)
D.text(s, ["1. 欠損を全体の中央値で埋める",
           "2. 全体でスケーリングする",
           "3. get_dummies で全体を符号化する",
           "4. **それから** train / test に分ける",
           "",
           "→ test の情報が前処理に混ざっている"],
       0.87, TOP + 0.85, 5.3, 1.8, 15, INK, line=1.38)
D.box(s, 6.83, TOP, 5.95, H, NAVY)
D.text(s, "✓ medprep の手順", 7.15, TOP + 0.28, 5.3, 0.4, 18, SKY)
D.text(s, ["1. **先に** train / test に分ける",
           "2. train だけで fit する",
           "3. test には transform しか当てない",
           "",
           "→ test に fit しようとすると **例外で止まる**"],
       7.15, TOP + 0.85, 5.3, 1.8, 15, PALEB, line=1.38)
D.code(s, ["# ★通ったことの証拠として残せる表である★",
           "mp.leak_check(rep.pipeline, rep.split.train, rep.split.test)",
           "",
           "  train/test の印        OK   train=train、test=test",
           "  fit に使った行          OK   train の行だけで fit している",
           "  目的変数が特徴量にないか  OK   目的変数 = 1年以内イベント",
           "  同じ患者が両側にないか   OK   重なりなし ← ★同一患者の複数行★"],
       L, TOP + H + 0.18, FULL, size=12.5)

# ============================================================ 18 保存
s = D.slide("結果の保存と再現性", "第1回・第2回で作った run{N} の規約をそのまま使う")
y1 = D.code(s, ["~/lab_output/Preprocessing/run{N}/",
                "├── table/   prep_tables.xlsx  table1.xlsx  run_info.json",
                "├── figure/  01_欠損の地図.png … 10_バランス.png",
                "├── model/   schema.yaml ★再現性の中核★  pipeline.pkl",
                "└── report/  prep_report.html（単一ファイル）",
                "",
                "~/lab_work/log/  history.csv（Excel でそのまま開ける）"],
            L, TOP, 7.70, size=13)
D.card(s, 8.45, TOP, 4.33, None, "過去の結果は上書きされない",
       ["table / model / figure が空でない",
        "run は決して使い回さない。",
        "Colab でランタイムが作り直されても、",
        "ドライブの最大番号から続ける。"], fill=PALE, hs=17, bs=14)
y = y1 + 0.32
D.text(s, "再現に必要なもの", L, y, 6.0, 0.35, 17, BLUE)
rows = [
    ["model/schema.yaml", "列の役割と、そう判断した理由。**これ 1 枚で前処理を再現できる**"],
    ["model/pipeline.pkl", "fit 済みの前処理（sklearn 互換）"],
    ["log/history.csv", "どのデータを、どのコードで、どう解析したかの一覧"],
]
y = D.table(s, ["ファイル", "中身"], rows, L, y + 0.42, [3.40, 8.83], rh=0.46, hh=0.48)
D.note(s, "結果フォルダの根には毎回 .gitignore（全無視）を置く。"
          "run には個票由来の出力が集まるので、誤って git に載せる事故を防ぐ。",
       L, y + 0.16, FULL, 0.4)

# ============================================================ 19 禁止事項
s = D.slide("この回でいちばん持ち帰ってほしいこと", "全自動にしてはいけないもの")
rows = [
    ["外れ値の自動削除", "医学では外れ値こそが重要な症例（劇症型・稀な合併症）でありうる"],
    ["目的変数の補完", "補完した目的変数で学習した結果は解釈できない。除外し、除外数を報告する"],
    ["ステップワイズ変数選択", "逐次選択は推定量に偏りを与え、信頼区間と p 値が無効になる"],
    ["p 値による Table 1 の解釈", "群間の偏りは p ではなく SMD で見る。p は例数で動く"],
    ["多重比較の無補正", "全列に p を出すので、BH の q 値を必ず併記する"],
    ["PH 仮定違反時の Cox の黙認", "Schoenfeld 検定が有意なら警告し、層別化か RMST を勧める"],
    ["施設・時期をそのまま特徴量に", "リークの温床。投入するかを人に問う"],
    ["欠損の指示変数を黙って落とす", "「測っていない」ことが予後情報である"],
]
y = D.table(s, ["項目", "理由"], rows, L, TOP, [4.30, 7.93], rh=0.42, hh=0.44)
y = D.callout(s, [[("このパッケージの値打ちは「何を自動化したか」と同じくらい、", PALEB),
                   ("「何を自動化しなかったか」で決まる。", WHITE, 17)]], L, y + 0.22, FULL)
D.note(s, "いずれも警告を出して人に返す。自動で処理しない。", L, y + 0.14, FULL, 0.34)

# ============================================================ 20 まとめ
s = D.slide("まとめ と この先", "自分のデータで動かすところまでが第3回")
y1 = D.card(s, L, TOP, 4.0, None, "今日やったこと",
            ["・ 汚れを見つける手順",
             "・ 判断を記録に残す（schema.yaml）",
             "・ リークを構造的に防ぐ",
             "・ run{N} に結果を残す"], fill=LIGHT, badge="✓", hs=17, bs=14)
y2 = D.card(s, 4.72, TOP, 4.0, None, "自分のデータで試す",
            ["1. USE_DEMO_DATA = False",
             "2. ID_COL / GROUP / DATE_COL を",
             "　 書き換える",
             "3. 上から実行する",
             "**同じコードが自分のデータでも動く**"], fill=PALE, hs=17, bs=14)
y3 = D.card(s, 8.89, TOP, 3.89, None, "第4回以降",
            ["・ MICE（多重代入）",
             "・ target encoding",
             "・ 競合リスク",
             "・ 時系列への接続"], fill=NAVY, head_color=SKY, body_color=PALEB,
            hs=17, bs=14)
y = max(y1, y2, y3) + 0.35
y = D.callout(s, [[("持ち帰るべき原理は 1 つ ―― ", PALEB),
                   ("自動化できる部分と、医学的判断が必要な部分の境界を知ること。",
                    WHITE, 17)]], L, y, FULL, fill=BLUE, color=WHITE)
D.text(s, ["質問は、いつでも。うまくいかないところこそ、いちばん学びがあります。"],
       L, y + 0.32, FULL, 0.5, 16, INK)
D.note(s, "medprep  https://github.com/kiwindow/medprep    "
          "日本腎・血液浄化AI学会 学術委員会  2026年9月", L, y + 1.00, FULL, 0.5)

out = os.path.join(os.path.dirname(os.path.abspath(__file__)),
                   "第3回_データの前処理_ver1_0.pptx")
os.makedirs(os.path.dirname(out), exist_ok=True)
D.save(out)
print(f"{D.n} slides ->", out)
